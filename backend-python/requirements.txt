# Core API framework
fastapi==0.136.3
uvicorn[standard]==0.49.0

# HTTP client (LLM calls, web fetches)
httpx==0.28.1

# Request body parsing (file uploads)
python-multipart==0.0.32

# Environment variable management
python-dotenv==1.2.2

# Background job scheduler
APScheduler==3.11.2

# Web search for tax law updates (optional — graceful fallback if absent)
duckduckgo-search==8.1.1
